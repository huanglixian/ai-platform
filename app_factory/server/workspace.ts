import fs from "node:fs/promises"; import path from "node:path"; import { spawn } from "node:child_process";
export function resolveWorkspacePath(root:string, relative:string){const base=path.resolve(root);const target=path.resolve(base,relative);if(target!==base&&!target.startsWith(`${base}${path.sep}`))throw new Error("Workspace path is outside project root");return target;}
export async function readWorkspaceFile(root:string,relative:string){return fs.readFile(resolveWorkspacePath(root,relative),"utf8");}
export async function writeWorkspaceFile(root:string,relative:string,content:string){const target=resolveWorkspacePath(root,relative);await fs.mkdir(path.dirname(target),{recursive:true});await fs.writeFile(target,content,"utf8");}
export function runWorkspaceCommand(root:string,command:string,timeoutMs=30_000){if(/(^|\s)(rm\s+-rf|sudo|mkfs|shutdown|docker\s+run)/i.test(command))throw new Error("Command rejected by workspace policy");return new Promise<{code:number|null;stdout:string;stderr:string}>((resolve,reject)=>{const child=spawn("/bin/sh",["-lc",command],{cwd:path.resolve(root),env:{PATH:process.env.PATH??"",NODE_ENV:"development"}});let stdout="",stderr="";const timer=setTimeout(()=>{child.kill("SIGTERM");reject(new Error("Command timed out"));},timeoutMs);child.stdout.on("data",d=>{stdout+=d.toString();if(stdout.length>200_000)child.kill("SIGTERM")});child.stderr.on("data",d=>{stderr+=d.toString();if(stderr.length>200_000)child.kill("SIGTERM")});child.on("error",reject);child.on("close",code=>{clearTimeout(timer);resolve({code,stdout,stderr})});});}
