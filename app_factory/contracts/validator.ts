import fs from "node:fs/promises"; import path from "node:path";
export type CheckResult={level:"error"|"warning"|"pass";message:string};
export async function validateProject(workspace:string):Promise<CheckResult[]>{const results:CheckResult[]=[];const file=path.join(workspace,"app.yaml");let raw:string;try{raw=await fs.readFile(file,"utf8")}catch{return [{level:"error",message:"缺少 app.yaml"}]};const doc:Record<string,unknown>={};for(const line of raw.split(/\r?\n/)){const match=line.match(/^([A-Za-z][\w-]*):\s*(.*)$/);if(match)doc[match[1]]=match[2].trim().replace(/^['"]|['"]$/g,"")}if(!Object.keys(doc).length)return [{level:"error",message:"app.yaml YAML 格式无效"}];for(const key of ["name","version","runtime","entry","healthPath"]){if(!doc[key])results.push({level:"error",message:`缺少字段 ${key}`})}if(!doc.capabilities)results.push({level:"warning",message:"capabilities 未配置，项目将以独立模式运行"});if(!results.some(r=>r.level==="error"))results.push({level:"pass",message:"app.yaml Contract 校验通过"});return results;}
