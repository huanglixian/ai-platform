import { apiError,apiOk } from "@/lib/server/api-response"; import { getProject } from "@/app_factory/server/database"; import { deploymentRepository,releaseRepository } from "@/app_factory/server/repositories"; import { startLocalDeployment,stopLocalDeployment,waitForHealth } from "@/app_factory/server/deployment";
export const runtime="nodejs";
export async function POST(_:Request,c:{params:Promise<{id:string}>}){const {id}=await c.params;const project=getProject(id);if(!project)return apiError("项目不存在",404);const release=releaseRepository.latest(id) as {id:string}|null;if(!release)return apiError("没有可部署的 Release",409);const runtime=startLocalDeployment(id,project.workspacePath);const healthy=await waitForHealth(runtime.url);const status=healthy?"running":"failed";deploymentRepository.create(id,release.id,status,runtime.url);if(!healthy)stopLocalDeployment(id);return healthy?apiOk({...runtime,health:"ok"}):apiError("部署健康检查失败",502,{url:runtime.url,logs:runtime.logs});}
export async function DELETE(_:Request,c:{params:Promise<{id:string}>}){const {id}=await c.params;if(!stopLocalDeployment(id))return apiError("部署未运行",404);deploymentRepository.updateStatus(id,"stopped");return apiOk({status:"stopped"});}
