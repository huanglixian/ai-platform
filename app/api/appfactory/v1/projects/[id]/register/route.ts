import { apiError,apiOk } from "@/lib/server/api-response"; import { getProject } from "@/app_factory/server/database"; import { releaseRepository } from "@/app_factory/server/repositories"; import { createAgentHubClient } from "@/app_factory/features/agenthub-client";
export const runtime="nodejs";
export async function POST(_:Request,c:{params:Promise<{id:string}>}){const {id}=await c.params;const project=getProject(id);if(!project)return apiError("项目不存在",404);const release=releaseRepository.latest(id) as {version:string}|null;if(!release)return apiError("没有可注册的 Release",409);try{const registered=await createAgentHubClient(project.agentHubMode as "disabled"|"local"|"http").registerApplication({name:project.name,description:project.description,producer:"appfactory",kind:"application",runtime:"nextjs",status:"active",entryUrl:`http://localhost:4100/${id}`,version:release.version,externalId:id});return apiOk({registered:true,application:registered});}catch(error){return apiError(error instanceof Error?error.message:"AgentHub 注册失败",502);}}
